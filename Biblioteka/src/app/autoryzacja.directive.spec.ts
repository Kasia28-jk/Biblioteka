import { AutoryzacjaDirective } from './autoryzacja.directive';

describe('AutoryzacjaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoryzacjaDirective();
    expect(directive).toBeTruthy();
  });
});
